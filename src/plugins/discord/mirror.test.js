// @flow

import Database from "better-sqlite3";
import {Mirror} from "./mirror";

describe("plugins/discord/mirror", () => {

    describe("retrieves earliest message", () => {
      
    })

    describe("retrieves latest message", () => {

    })

    describe("fetches messages before earliest fetch id", () => {

    })

    describe("fetches messages after latest fetch id", () => {

    })

})
